# 酒店管理系统需求分析、功能模块与数据库设计

## 一、需求分析

### 1.1 项目背景

随着酒店日常业务量增加，如果仍然通过人工方式记录房间、用户和订单信息，容易出现查询不方便、数据混乱、统计困难等问题。为了提高酒店基础业务管理效率，本项目设计一个简化版酒店管理系统。

本系统主要面向酒店房间管理和用户订房场景，支持管理员维护房间信息，普通用户查询并预订房间，管理员查看和处理订单，同时提供简单的数据统计功能。

本系统不追求复杂业务流程，主要完成数据库课程实践中对数据库设计、基本增删改查、条件查询、角色权限和简单统计的要求。

------

### 1.2 系统目标

本系统的目标如下：

| 序号 | 系统目标           | 说明                                                         |
| ---- | ------------------ | ------------------------------------------------------------ |
| 1    | 实现用户登录       | 用户输入用户名和密码后登录系统                               |
| 2    | 实现角色区分       | 系统区分管理员和普通用户，不同角色进入不同页面               |
| 3    | 实现房间管理       | 管理员可以新增、修改、删除、查询房间信息                     |
| 4    | 实现房间查询       | 用户可以按房型、价格、状态等条件查询房间                     |
| 5    | 实现房间预订       | 普通用户可以选择空闲房间进行预订                             |
| 6    | 实现订单管理       | 管理员可以查看和处理订单，用户可以查看自己的订单             |
| 7    | 实现简单统计       | 管理员可以查看房间状态、订单状态和收入统计                   |
| 8    | 保证数据库结构清晰 | 通过主键、外键、索引、视图、序列、触发器等数据库对象完成数据库设计 |

------

### 1.3 用户角色分析

系统主要包含两类用户：管理员和普通用户。

| 角色     | 说明             | 主要权限                                           |
| -------- | ---------------- | -------------------------------------------------- |
| 管理员   | 酒店后台管理人员 | 管理房间、查看所有订单、修改订单状态、查看统计信息 |
| 普通用户 | 酒店订房用户     | 查询房间、预订房间、查看自己的订单、取消自己的订单 |

------

## 二、功能模块设计

### 2.1 功能模块总览

本系统主要包括以下功能模块：

| 模块名称     | 使用角色         | 功能说明                                 |
| ------------ | ---------------- | ---------------------------------------- |
| 用户登录模块 | 管理员、普通用户 | 用户登录、角色判断、退出登录             |
| 房间管理模块 | 管理员           | 对房间信息进行新增、修改、删除和查询     |
| 房间查询模块 | 管理员、普通用户 | 按房型、价格、状态等条件查询房间         |
| 房间预订模块 | 普通用户         | 用户选择空闲房间并提交预订               |
| 订单管理模块 | 管理员、普通用户 | 管理员处理所有订单，用户查看自己的订单   |
| 简单统计模块 | 管理员           | 查看房间状态统计、订单状态统计和收入统计 |

------

## 三、详细功能说明

### 3.1 用户登录模块

| 功能名称 | 使用角色         | 功能说明                               | 输入内容     | 输出结果                 |
| -------- | ---------------- | -------------------------------------- | ------------ | ------------------------ |
| 用户登录 | 管理员、普通用户 | 用户输入用户名和密码，系统校验账号信息 | 用户名、密码 | 登录成功或失败提示       |
| 角色判断 | 系统自动完成     | 登录成功后，根据用户角色进入不同页面   | 用户角色     | 管理员页面或普通用户页面 |
| 退出登录 | 管理员、普通用户 | 用户退出当前系统                       | 点击退出按钮 | 返回登录页面             |

------

### 3.2 房间管理模块

该模块主要由管理员使用，用于维护酒店房间基础信息。

| 功能名称     | 使用角色 | 功能说明               | 输入内容                       | 输出结果           |
| ------------ | -------- | ---------------------- | ------------------------------ | ------------------ |
| 查看房间列表 | 管理员   | 查看系统中所有房间信息 | 无                             | 房间列表           |
| 添加房间     | 管理员   | 新增一条房间信息       | 房间号、房型、价格、状态、备注 | 添加成功或失败提示 |
| 修改房间     | 管理员   | 修改已有房间的信息     | 房间编号、修改后的房间信息     | 修改成功或失败提示 |
| 删除房间     | 管理员   | 删除不需要的房间信息   | 房间编号                       | 删除成功或失败提示 |
| 查询房间     | 管理员   | 按条件查询房间         | 房型、价格、状态等             | 符合条件的房间列表 |

------

### 3.3 房间查询模块

该模块管理员和普通用户都可以使用，普通用户主要用于查找可以预订的房间。

| 功能名称   | 使用角色         | 功能说明                                   | 查询条件             | 查询结果       |
| ---------- | ---------------- | ------------------------------------------ | -------------------- | -------------- |
| 按房型查询 | 管理员、普通用户 | 根据房型查询房间，如单人间、双人间、豪华间 | 房型                 | 房间列表       |
| 按价格查询 | 管理员、普通用户 | 根据价格范围查询房间                       | 最低价格、最高价格   | 房间列表       |
| 按状态查询 | 管理员、普通用户 | 根据房间状态查询房间                       | 空闲、已预订、维修中 | 房间列表       |
| 模糊查询   | 管理员、普通用户 | 根据房间号或备注关键词查询房间             | 关键词               | 匹配的房间列表 |
| 空闲房查询 | 普通用户         | 查询当前可以预订的房间                     | 房间状态为空闲       | 空闲房间列表   |

------

### 3.4 房间预订模块

该模块主要由普通用户使用。用户登录后，可以查看空闲房间并提交预订。

| 功能名称     | 使用角色     | 功能说明                         | 输入内容                     | 输出结果           |
| ------------ | ------------ | -------------------------------- | ---------------------------- | ------------------ |
| 查看空闲房间 | 普通用户     | 查看当前可以预订的房间           | 无或查询条件                 | 空闲房间列表       |
| 提交预订     | 普通用户     | 用户选择房间后提交预订信息       | 房间编号、入住日期、离店日期 | 生成订单           |
| 自动生成订单 | 系统自动完成 | 用户预订成功后，系统生成订单记录 | 用户编号、房间编号、日期信息 | 新增订单           |
| 修改房间状态 | 系统自动完成 | 预订成功后，将房间状态改为已预订 | 房间编号                     | 房间状态更新       |
| 取消预订     | 普通用户     | 用户取消尚未完成的订单           | 订单编号                     | 订单状态改为已取消 |

------

### 3.5 订单管理模块

订单管理模块用于处理用户预订后的订单信息。管理员可以查看全部订单，普通用户只能查看自己的订单。

| 功能名称       | 使用角色         | 功能说明                             | 输入内容                       | 输出结果           |
| -------------- | ---------------- | ------------------------------------ | ------------------------------ | ------------------ |
| 查看我的订单   | 普通用户         | 普通用户查看自己的订房记录           | 当前登录用户编号               | 个人订单列表       |
| 查看所有订单   | 管理员           | 管理员查看系统中所有订单             | 无或查询条件                   | 全部订单列表       |
| 确认订单       | 管理员           | 管理员确认用户提交的订单             | 订单编号                       | 订单状态改为已确认 |
| 完成订单       | 管理员           | 用户退房或订单结束后，管理员完成订单 | 订单编号                       | 订单状态改为已完成 |
| 取消订单       | 普通用户、管理员 | 用户或管理员取消订单                 | 订单编号                       | 订单状态改为已取消 |
| 删除订单       | 管理员           | 管理员删除无效或测试订单             | 订单编号                       | 删除成功或失败提示 |
| 按状态查询订单 | 管理员           | 根据订单状态筛选订单                 | 待确认、已确认、已完成、已取消 | 订单列表           |

------

### 3.6 简单统计模块

该模块主要由管理员使用，用于展示系统中的基础统计信息。

| 功能名称     | 使用角色 | 功能说明               | 统计依据                       | 输出结果                     |
| ------------ | -------- | ---------------------- | ------------------------------ | ---------------------------- |
| 房间状态统计 | 管理员   | 统计不同状态的房间数量 | rooms 表中的 status 字段       | 空闲、已预订、维修中房间数量 |
| 订单状态统计 | 管理员   | 统计不同状态的订单数量 | orders 表中的 status 字段      | 各状态订单数量               |
| 收入统计     | 管理员   | 统计已完成订单的总金额 | orders 表中的 total_price 字段 | 总收入金额                   |

------

## 四、业务流程说明

### 4.1 管理员使用流程

```
管理员登录系统
    ↓
进入管理员首页
    ↓
维护房间信息
    ↓
查看用户订单
    ↓
确认订单 / 完成订单 / 取消订单
    ↓
查看统计信息
```

------

### 4.2 普通用户使用流程

```
普通用户登录系统
    ↓
查看空闲房间
    ↓
按房型、价格等条件查询房间
    ↓
选择房间并提交预订
    ↓
查看我的订单
    ↓
必要时取消订单
```

------

## 五、数据库设计

### 5.1 数据库设计概述

本系统采用 Oracle 数据库。为了降低系统复杂度，数据库只设计三张核心表：

| 表名   | 中文名称 | 作用                     |
| ------ | -------- | ------------------------ |
| users  | 用户表   | 存储管理员和普通用户信息 |
| rooms  | 房间表   | 存储酒店房间信息         |
| orders | 订单表   | 存储用户预订订单信息     |

三张表之间的关系如下：

| 关系            | 说明                                                 |
| --------------- | ---------------------------------------------------- |
| users 和 orders | 一个用户可以有多个订单，一个订单只属于一个用户       |
| rooms 和 orders | 一个房间可以产生多个历史订单，一个订单只对应一个房间 |

------

## 六、数据表设计

### 6.1 用户表 users

用户表用于保存系统用户信息，包括管理员和普通用户。

| 字段名    | 数据类型     | 约束       | 说明                                          |
| --------- | ------------ | ---------- | --------------------------------------------- |
| user_id   | NUMBER       | 主键       | 用户编号                                      |
| username  | VARCHAR2(50) | 唯一，非空 | 用户名                                        |
| password  | VARCHAR2(50) | 非空       | 用户密码                                      |
| real_name | VARCHAR2(50) |            | 真实姓名                                      |
| role      | VARCHAR2(20) | 非空       | 用户角色，admin 表示管理员，user 表示普通用户 |

建表 SQL：

```
CREATE TABLE users (
    user_id NUMBER PRIMARY KEY,
    username VARCHAR2(50) UNIQUE NOT NULL,
    password VARCHAR2(50) NOT NULL,
    real_name VARCHAR2(50),
    role VARCHAR2(20) NOT NULL
);
```

------

### 6.2 房间表 rooms

房间表用于保存酒店房间信息。

| 字段名    | 数据类型      | 约束       | 说明                       |
| --------- | ------------- | ---------- | -------------------------- |
| room_id   | NUMBER        | 主键       | 房间编号                   |
| room_no   | VARCHAR2(20)  | 唯一，非空 | 房间号                     |
| room_type | VARCHAR2(50)  | 非空       | 房间类型，如单人间、双人间 |
| price     | NUMBER(10,2)  | 非空       | 房间价格                   |
| status    | VARCHAR2(20)  | 非空       | 房间状态                   |
| remark    | VARCHAR2(200) |            | 备注信息                   |

房间状态说明：

| 状态   | 说明                   |
| ------ | ---------------------- |
| 空闲   | 当前房间可以预订       |
| 已预订 | 当前房间已经被用户预订 |
| 维修中 | 当前房间暂时不能使用   |

建表 SQL：

```
CREATE TABLE rooms (
    room_id NUMBER PRIMARY KEY,
    room_no VARCHAR2(20) UNIQUE NOT NULL,
    room_type VARCHAR2(50) NOT NULL,
    price NUMBER(10,2) NOT NULL,
    status VARCHAR2(20) DEFAULT '空闲' NOT NULL,
    remark VARCHAR2(200)
);
```

------

### 6.3 订单表 orders

订单表用于保存用户预订房间后产生的订单信息。

| 字段名      | 数据类型     | 约束         | 说明         |
| ----------- | ------------ | ------------ | ------------ |
| order_id    | NUMBER       | 主键         | 订单编号     |
| user_id     | NUMBER       | 外键         | 用户编号     |
| room_id     | NUMBER       | 外键         | 房间编号     |
| start_date  | DATE         | 非空         | 入住日期     |
| end_date    | DATE         | 非空         | 离店日期     |
| total_price | NUMBER(10,2) |              | 订单总价     |
| status      | VARCHAR2(20) | 非空         | 订单状态     |
| create_time | DATE         | 默认当前时间 | 订单创建时间 |

订单状态说明：

| 状态   | 说明                         |
| ------ | ---------------------------- |
| 待确认 | 用户提交预订后生成的初始状态 |
| 已确认 | 管理员确认订单               |
| 已完成 | 订单已经结束                 |
| 已取消 | 用户或管理员取消订单         |

建表 SQL：

```
CREATE TABLE orders (
    order_id NUMBER PRIMARY KEY,
    user_id NUMBER NOT NULL,
    room_id NUMBER NOT NULL,
    start_date DATE NOT NULL,
    end_date DATE NOT NULL,
    total_price NUMBER(10,2),
    status VARCHAR2(20) DEFAULT '待确认' NOT NULL,
    create_time DATE DEFAULT SYSDATE,
    CONSTRAINT fk_orders_user 
        FOREIGN KEY (user_id) REFERENCES users(user_id),
    CONSTRAINT fk_orders_room 
        FOREIGN KEY (room_id) REFERENCES rooms(room_id)
);
```

------

## 七、数据库关系设计

### 7.1 表关系说明

| 表关系         | 关系类型 | 说明                         |
| -------------- | -------- | ---------------------------- |
| users → orders | 一对多   | 一个用户可以提交多个订单     |
| rooms → orders | 一对多   | 一个房间可以对应多个历史订单 |

------

### 7.2 ER 图文字说明

```
用户 users
  user_id
  username
  password
  real_name
  role
      |
      | 1 对 多
      |
订单 orders
  order_id
  user_id
  room_id
  start_date
  end_date
  total_price
  status
  create_time
      |
      | 多 对 1
      |
房间 rooms
  room_id
  room_no
  room_type
  price
  status
  remark
```

------

## 八、数据库对象设计

### 8.1 序列设计

Oracle 中可以使用序列生成主键编号。

```
CREATE SEQUENCE seq_users START WITH 1 INCREMENT BY 1;
CREATE SEQUENCE seq_rooms START WITH 1 INCREMENT BY 1;
CREATE SEQUENCE seq_orders START WITH 1 INCREMENT BY 1;
```

序列说明：

| 序列名     | 对应表 | 作用         |
| ---------- | ------ | ------------ |
| seq_users  | users  | 生成用户编号 |
| seq_rooms  | rooms  | 生成房间编号 |
| seq_orders | orders | 生成订单编号 |

------

### 8.2 索引设计

为了提高查询效率，在常用查询字段上建立索引。

```
CREATE INDEX idx_rooms_type ON rooms(room_type);
CREATE INDEX idx_rooms_status ON rooms(status);
CREATE INDEX idx_orders_user ON orders(user_id);
CREATE INDEX idx_orders_room ON orders(room_id);
CREATE INDEX idx_orders_status ON orders(status);
```

索引说明：

| 索引名            | 所属表 | 字段      | 作用                       |
| ----------------- | ------ | --------- | -------------------------- |
| idx_rooms_type    | rooms  | room_type | 提高按房型查询房间的效率   |
| idx_rooms_status  | rooms  | status    | 提高按房间状态查询的效率   |
| idx_orders_user   | orders | user_id   | 提高用户查看个人订单的效率 |
| idx_orders_room   | orders | room_id   | 提高按房间查询订单的效率   |
| idx_orders_status | orders | status    | 提高按订单状态查询的效率   |

------

### 8.3 视图设计

为了方便管理员查看订单详情，创建订单详情视图。

```
CREATE VIEW v_order_detail AS
SELECT 
    o.order_id,
    u.username,
    u.real_name,
    r.room_no,
    r.room_type,
    r.price,
    o.start_date,
    o.end_date,
    o.total_price,
    o.status,
    o.create_time
FROM orders o
JOIN users u ON o.user_id = u.user_id
JOIN rooms r ON o.room_id = r.room_id;
```

视图说明：

| 视图名         | 作用                                               |
| -------------- | -------------------------------------------------- |
| v_order_detail | 将订单表、用户表和房间表连接起来，方便查看订单详情 |

------

### 8.4 触发器设计

创建订单后，自动将对应房间状态修改为“已预订”。

```
CREATE OR REPLACE TRIGGER trg_after_order_insert
AFTER INSERT ON orders
FOR EACH ROW
BEGIN
    UPDATE rooms
    SET status = '已预订'
    WHERE room_id = :NEW.room_id;
END;
/
```

触发器说明：

| 触发器名               | 触发时机            | 作用                               |
| ---------------------- | ------------------- | ---------------------------------- |
| trg_after_order_insert | orders 表插入数据后 | 订单创建后自动修改房间状态为已预订 |

------

## 九、测试数据设计

### 9.1 用户测试数据

```
INSERT INTO users VALUES (seq_users.NEXTVAL, 'admin', '123456', '管理员', 'admin');
INSERT INTO users VALUES (seq_users.NEXTVAL, 'user01', '123456', '张三', 'user');
INSERT INTO users VALUES (seq_users.NEXTVAL, 'user02', '123456', '李四', 'user');
```

------

### 9.2 房间测试数据

```
INSERT INTO rooms VALUES (seq_rooms.NEXTVAL, '101', '单人间', 188, '空闲', '普通单人间');
INSERT INTO rooms VALUES (seq_rooms.NEXTVAL, '102', '单人间', 188, '空闲', '靠近电梯');
INSERT INTO rooms VALUES (seq_rooms.NEXTVAL, '201', '双人间', 288, '空闲', '适合两人入住');
INSERT INTO rooms VALUES (seq_rooms.NEXTVAL, '301', '豪华间', 388, '维修中', '设备维护');
```

------

### 9.3 订单测试数据

```
INSERT INTO orders (
    order_id, user_id, room_id, start_date, end_date, total_price, status, create_time
) VALUES (
    seq_orders.NEXTVAL,
    2,
    1,
    TO_DATE('2026-05-01', 'YYYY-MM-DD'),
    TO_DATE('2026-05-03', 'YYYY-MM-DD'),
    376,
    '待确认',
    SYSDATE
);
```

------

## 十、主要查询 SQL 设计

### 10.1 查询所有房间

```
SELECT * FROM rooms;
```

------

### 10.2 查询所有空闲房间

```
SELECT * FROM rooms WHERE status = '空闲';
```

------

### 10.3 按房型查询房间

```
SELECT * FROM rooms WHERE room_type = '单人间';
```

------

### 10.4 按价格范围查询房间

```
SELECT * FROM rooms WHERE price BETWEEN 100 AND 300;
```

------

### 10.5 模糊查询房间

```
SELECT * 
FROM rooms
WHERE room_no LIKE '%10%'
   OR remark LIKE '%普通%';
```

------

### 10.6 查询用户自己的订单

```
SELECT * 
FROM v_order_detail
WHERE username = 'user01';
```

------

### 10.7 查询所有待确认订单

```
SELECT *
FROM v_order_detail
WHERE status = '待确认';
```

------

### 10.8 统计不同状态的房间数量

```
SELECT status, COUNT(*) AS room_count
FROM rooms
GROUP BY status;
```

------

### 10.9 统计不同状态的订单数量

```
SELECT status, COUNT(*) AS order_count
FROM orders
GROUP BY status;
```

------

### 10.10 统计已完成订单总收入

```
SELECT NVL(SUM(total_price), 0) AS total_income
FROM orders
WHERE status = '已完成';
```

------

## 十一、功能模块与数据库表对应关系

| 功能模块     | 涉及数据表             | 说明                             |
| ------------ | ---------------------- | -------------------------------- |
| 用户登录模块 | users                  | 校验用户名、密码和角色           |
| 房间管理模块 | rooms                  | 对房间信息进行增删改查           |
| 房间查询模块 | rooms                  | 按房型、价格、状态查询房间       |
| 房间预订模块 | users、rooms、orders   | 用户预订房间并生成订单           |
| 订单管理模块 | orders、v_order_detail | 查询订单、修改订单状态           |
| 简单统计模块 | rooms、orders          | 统计房间状态、订单状态和订单收入 |

------

## 十二、页面设计简述

为了实现上述功能，系统页面可以简化为以下几个页面：

| 页面名称         | 使用角色         | 页面功能                               |
| ---------------- | ---------------- | -------------------------------------- |
| 登录页面         | 管理员、普通用户 | 输入用户名和密码登录                   |
| 管理员首页       | 管理员           | 显示房间管理、订单管理和统计入口       |
| 房间管理页面     | 管理员           | 新增、修改、删除、查询房间             |
| 用户房间查询页面 | 普通用户         | 查询空闲房间并提交预订                 |
| 订单管理页面     | 管理员、普通用户 | 管理员查看全部订单，用户查看自己的订单 |
| 统计页面         | 管理员           | 显示房间状态、订单状态和收入统计       |

------

## 十三、本部分小结

本系统采用简化设计，主要实现酒店管理中最基础的房间管理和订房流程。系统包含管理员和普通用户两种角色，管理员负责维护房间信息、处理订单和查看统计数据，普通用户负责查询房间和提交预订。

数据库部分只设计 users、rooms、orders 三张核心表，降低了系统实现难度，同时通过主键、外键、序列、索引、视图和触发器等数据库对象保证数据库结构完整，能够满足课程设计对数据库实践的基本要求

------

## 十四、系统实现说明

### 14.1 实现技术说明

为了控制课程设计实现难度，同时保证系统功能完整，本系统最终采用普通 Java 项目方式进行开发，不使用 Spring Boot 等大型框架，主要技术如下：

| 技术/工具 | 用途说明 |
| --------- | -------- |
| Java      | 编写系统业务逻辑、菜单交互和数据处理 |
| JDBC      | 连接 Oracle 数据库，执行 SQL 语句 |
| Oracle    | 存储用户、房间、订单等业务数据 |
| SQL       | 完成建表、索引、视图、触发器和测试数据初始化 |
| 控制台菜单 | 用于模拟系统页面和功能入口 |

该实现方式结构清晰，便于展示数据库课程设计中“数据库设计 + 基本业务功能实现”的核心要求。

------

### 14.2 系统实现结构

项目采用分层思路实现，主要包括入口层、业务层、数据访问层、实体层和工具层。

| 层次/目录 | 作用说明 |
| --------- | -------- |
| Main.java | 系统主入口，负责菜单显示、登录流程和用户交互 |
| entity    | 实体类目录，对应 users、rooms、orders 查询结果 |
| dao       | 数据访问层，负责执行数据库增删改查 |
| service   | 业务逻辑层，负责登录、房间管理、预订、订单处理、统计等业务规则 |
| config    | 数据库连接配置信息 |
| util      | 工具类，如数据库连接、日期处理、参数校验 |
| sql       | 数据库脚本目录，包含建库脚本、测试数据脚本和查询示例脚本 |

------

### 14.3 主要功能与代码实现对应关系

| 功能模块 | 主要实现类/文件 | 说明 |
| -------- | --------------- | ---- |
| 登录与角色判断 | Main.java、AuthService.java、UserDao.java | 完成账号校验并区分管理员和普通用户 |
| 房间管理 | Main.java、RoomService.java、RoomDao.java | 完成房间新增、修改、删除、查询 |
| 房间查询 | Main.java、RoomService.java、RoomDao.java | 支持按房型、价格、状态、关键字查询 |
| 房间预订 | Main.java、OrderService.java、OrderDao.java | 普通用户选择空闲房间后提交订单 |
| 订单管理 | Main.java、OrderService.java、OrderDao.java | 支持查看订单、确认订单、完成订单、取消订单、删除订单 |
| 统计功能 | Main.java、RoomDao.java、OrderDao.java | 统计房间状态、订单状态和已完成订单收入 |
| 数据库对象 | schema.sql | 定义表、序列、索引、视图、触发器 |

------

## 十五、系统运行与部署说明

### 15.1 运行环境

| 项目 | 说明 |
| ---- | ---- |
| 操作系统 | Windows |
| JDK | JDK 8 及以上版本 |
| 数据库 | Oracle |
| JDBC 驱动 | ojdbc8.jar |

------

### 15.2 运行步骤

1. 安装 Oracle 数据库并创建可用数据库用户。
2. 打开 `sql/schema.sql`，执行建表、序列、索引、视图和触发器脚本。
3. 打开 `sql/init_data.sql`，执行初始化测试数据脚本。
4. 将 `ojdbc8.jar` 放入项目 `lib` 目录。
5. 修改 `src/com/hotel/config/DatabaseConfig.java` 中的数据库地址、用户名和密码。
6. 编译项目：

```powershell
javac -encoding UTF-8 -cp "lib/*" -d out (Get-ChildItem -Recurse -Filter *.java src | ForEach-Object { $_.FullName })
```

7. 运行项目：

```powershell
java -cp "out;lib/*" com.hotel.Main
```

------

### 15.3 默认测试账号

| 用户类型 | 用户名 | 密码 |
| -------- | ------ | ---- |
| 管理员   | admin  | 123456 |
| 普通用户 | user01 | 123456 |
| 普通用户 | user02 | 123456 |

------

## 十六、系统测试说明

本系统测试以“功能是否完整、业务流程是否正确、数据库对象是否能支持业务实现”为主要目标。测试主要围绕以下内容展开：

| 测试类别 | 测试内容 |
| -------- | -------- |
| 登录测试 | 用户名密码校验、角色区分是否正确 |
| 房间管理测试 | 房间新增、修改、删除、查询是否正常 |
| 房间查询测试 | 房型查询、价格查询、状态查询、模糊查询是否正确 |
| 预订测试 | 空闲房预订、订单生成、房间状态变化是否正确 |
| 订单管理测试 | 查看订单、确认订单、完成订单、取消订单、删除订单是否正确 |
| 统计测试 | 房间状态统计、订单状态统计、收入统计是否正确 |
| 异常测试 | 非法输入、重复房间号、错误日期、错误状态等是否能够拦截 |

为了便于课程答辩和验收，项目额外提供了独立的测试用例文档，可作为测试记录和展示材料使用。

------

## 十七、系统特点与不足

### 17.1 系统特点

1. 结构简单清晰，适合作为数据库课程设计项目展示。
2. 业务功能完整，覆盖登录、房间管理、预订、订单管理和统计等核心流程。
3. 数据库对象较完整，包含主键、外键、索引、视图、序列、触发器等设计内容。
4. 对常见输入错误进行了校验，如空值、非法数字、非法日期、重复房间号和错误状态值。

------

### 17.2 存在不足

1. 当前系统采用控制台方式实现，界面展示效果不如图形界面或网页系统直观。
2. 用户密码采用明文保存，安全性较弱，实际项目中应进行加密处理。
3. 本系统主要面向课程设计演示，复杂业务场景和高并发场景考虑较少。
4. 房间状态与订单状态的联动逻辑仍可继续优化，使其在更复杂历史订单场景下更加严谨。

------

## 十八、交付材料清单

为了满足课程设计提交和演示需要，本项目建议最终提交以下材料：

| 序号 | 交付材料 | 说明 |
| ---- | -------- | ---- |
| 1 | 项目源代码 | 包含 Java 源码和项目目录结构 |
| 2 | 数据库建库脚本 | `sql/schema.sql` |
| 3 | 数据初始化脚本 | `sql/init_data.sql` |
| 4 | 主要查询示例脚本 | `sql/query_demo.sql` |
| 5 | 需求分析与数据库设计文档 | 即本说明文档 |
| 6 | 测试用例文档 | 用于展示功能测试覆盖情况 |
| 7 | 系统运行截图 | 登录、房间管理、预订、订单管理、统计等截图 |
| 8 | 课程设计总结 | 简述设计思路、实现过程和问题分析 |

------

## 十九、答辩演示建议

为了使演示过程更连贯，建议按以下顺序进行：

1. 展示数据库表结构、视图、触发器和测试数据。
2. 使用管理员账号登录，演示房间查询、新增房间、查看订单和统计功能。
3. 使用普通用户账号登录，演示查询空闲房间、提交预订、查看我的订单和取消订单。
4. 返回管理员账号，演示确认订单、完成订单和最终统计结果变化。
5. 最后总结系统实现技术、数据库设计思路及当前不足。
